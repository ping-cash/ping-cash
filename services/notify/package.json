{
  "name": "@ping/notify-service",
  "version": "0.1.0",
  "private": true,
  "description": "Multi-channel notification orchestration: WhatsApp, SMS, Push",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch --onSuccess \"node dist/index.js\"",
    "start": "node dist/index.js",
    "test": "vitest run --passWithNoTests",
    "typecheck": "tsc --noEmit",
    "lint": "eslint src/",
    "clean": "rm -rf dist",
    "lint:fix": "eslint src/ --fix"
  },
  "dependencies": {
    "@ping/config": "workspace:*",
    "@ping/types": "workspace:*",
    "@ping/utils": "workspace:*",
    "@fastify/cors": "^9.0.0",
    "@fastify/helmet": "^11.0.0",
    "@fastify/rate-limit": "^9.0.0",
    "fastify": "^4.26.0",
    "ioredis": "^5.3.0",
    "pino": "^8.19.0",
    "twilio": "^5.0.0",
    "zod": "^3.22.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.3.0"
  }
}
