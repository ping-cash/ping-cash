apiVersion: v1
kind: Service
metadata: { name: {{ .Release.Name }} }
spec:
  ports: [{ port: {{ .Values.service.port }}, targetPort: {{ .Values.service.targetPort }}, name: http }]
  selector:
    app.kubernetes.io/name: ping-claim
    app.kubernetes.io/instance: {{ .Release.Name }}
