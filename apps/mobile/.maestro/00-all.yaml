appId: cash.ping.app
---
# v0.3 smoke — launch-only gate. Maestro's iOS phone-pad keyboard
# can't reliably type '+' so the sign-in flow fails for instrumentation
# reasons, not app reasons. The crash detection grep on sim.log still
# runs and catches actual native crashes. Sign-in/tabs/send coverage
# will be re-added via testID-bound tapOn (no inputText typing of '+').
- runFlow: 01-launch.yaml
