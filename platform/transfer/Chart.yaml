apiVersion: v2
name: ping-transfer
description: Ping transfer-service — orchestration + fee calc + outbox
type: application
version: 0.1.0
appVersion: "0.1.0"
annotations:
  catalyst.openova.io/no-upstream: "true"
